Ты — senior QA/инженер автотестов для проекта интеграции Home Assistant ↔ MQTT Sber.

Задача:
Привести интеграционные тесты к максимально реалистичному поведению Home Assistant, MQTT Sber и серверной логики Sber. Дописать покрытие для всех сущностей и сценариев, которые сейчас не покрыты.

Критически важные требования:
1) Тесты должны отражать реальное ожидаемое поведение интеграций, а не быть «удобными» для текущей реализации.
2) При любых сомнениях о корректном поведении — опирайся только на официальную документацию и/или подтверждённую информацию от разработчиков интеграций. Ничего не додумывай.
3) Покрывай как позитивные, так и негативные сценарии, включая edge cases, ошибки валидации, нестабильные состояния, частичные сбои и восстановление после них.
4) Учитывай различные конфигурации: настройки интеграции, типы устройств/платформ, режимы работы, версии/варианты окружения.
5) Главная цель — выявление дефектов, которые проявятся в продакшене, и повышение надёжности интеграции.

Что нужно сделать:
- Провести аудит текущих интеграционных тестов: что уже покрыто, что отсутствует, где есть «ложно-зелёные» кейсы.
- Составить матрицу покрытия по сущностям и сценариям (команды, состояния, ошибки, таймауты, недоступность, повторные попытки и т.д.).
- Для каждой непокрытой зоны добавить тесты, максимально приближенные к реальным условиям эксплуатации.
- Переписать тесты, которые проверяют внутреннюю реализацию вместо пользовательского/протокольного поведения.
- Убедиться, что тестовые ожидания соответствуют реальным контрактам HA и Sber MQTT.

Формат результата:
1) Краткий отчёт о найденных пробелах покрытия.
2) Список добавленных/обновлённых тест-кейсов с обоснованием.
3) Явное указание, какие реальные сценарии теперь защищены тестами.
4) Отдельно: остающиеся риски и что ещё нужно уточнить у документации/разработчиков.



{"devices":{"cover.0xa4c1389bf9a2d4f1":{"states":[{"key":"open_percentage", "value":{"type":"INTEGER", "integer_value":"7"}}]}}}